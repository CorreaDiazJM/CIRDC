<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/stylesheets/documentos/home.css">
        <link rel="stylesheet" href="/stylesheets/necolas.github.io_normalize.css_8.0.1_normalize.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Almarai&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Tektur&display=swap" rel="stylesheet">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500&display=swap" rel="stylesheet">
        <title>CIRDC</title>
    </head>
    <body>
        <header class="header">
            <a class="logo" style="user-select: none;">CIRDC</a>

            <nav class="navC">
                <a href="/documentos">Documentos</a>
                <a href="/usuarios/logout" class="nav_a">Cerrar Sesion</a>
            </nav>
        </header>

        <div class="informes">
            <div class="informe">
                <h2><%= documento.titulo_doc %></h2>
                <p><%= documento.contenido_doc %></p>
            </div>

            <br><br>

        </div>

        <% for(var i = 0; i < comentarios.length; i++) { %>
            <div class="comentario in1">
                <h2 class="comen"><%= comentarios[i].apellido_usu %> <%= comentarios[i].nombre_usu %></h2>
                <p class="comen_p"><%= comentarios[i].comentario_com %></p>
            </div>
        <% } %>

        <br><br>

        <div class="informe">
            <form action=<%= '/comentarios/' + documento.id_doc + '/' + usuario %> method="POST">
                <textarea name="comentario" id="comentario" placeholder="Comenta" cols="30" rows="10" required></textarea>
                <button type="submit" class="comentar_button">Comentar</button>
            </form>
        </div>
    </body>
</html>
